import React from 'react';
import styled from 'styled-components';

const Elem = styled.div`
    font-size: 4rem;
    color: #baa;
`;

export const Splash = () => {
    return <Elem>STRELKA</Elem>;
};
